# Файл тепер буде порожнім, оскільки ми використовуємо тільки JWT
# Всі SQL схеми для auth_tokens видалені

class AuthSchemes:
    """Клас для організації SQL схем авторизації (тепер порожній - використовуємо JWT)"""
    pass 